amqp:
  url: 'amqp://rabbitmq'
github:
  port: 4567
  secret: ${HERMAN_GH_SECRET}
  images:
    - url: git@github.com:AGhost-7/test-repo.git
      name: test-image
git:
  dir: /var/lib/herman/git
docker:
  socketPath: /run/docker.sock
  registry: localhost:5000
